server:
  port: 9002
spring:
  application:
    name: cloud-nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
management:
  endpoints:
    web:
      exposure:
        include: '*'  #监控